let x = {};
// let x = new Object();
// Mỗi đối tượng trong javascript có một method gọi là constructor
// nó dùng để được tạo đối tượng đó.

// Thay vì new String(); thì nhờ có thăng constructor này 
// thì ta có thể viết là '', "", ``

// Tương tự new Boolean(); // true, false
// new Number();// 1, 2, 3....


